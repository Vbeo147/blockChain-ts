{
  "include": ["src"], // 컴파일을 하기 위한 파일위치
  "compilerOptions": {
    "outDir": "build", // 빌드파일 위치
    "target": "ES6", // 컴파일되는 자바스크립트 버전
    "lib": ["ES6"], // 코드가 실행될 환경을 설정
    // DOM : 브라우저에서도 실행될 수 있게함.
    "strict": true, // 타입스크립트가 더욱 엄격해짐
    "allowJs": true
  }
}
